// Selectores del DOM

const heading = document.querySelector('#heading');//querySelector solo selecciona un elemento 
/*
console.log(heading);
console.log(heading.textContent);
console.log(heading.tagName);
console.log(heading.classList);
console.log(heading.id);
*/

const enlaces = document.querySelectorAll('.navegacion a');//selecciona una coleccion de elementos
console.log(enlaces);